# Aloudata 指标/维度分析配置面板设计验收

final result: blocked

## 对照目标

- 来源视觉：`/var/folders/64/h6k4c1q17f5725r8cgxccdmr0000gn/T/codex-clipboard-d854f78d-24be-4c17-ac58-48dae82e3fe0.png`（873 × 643 px）。
- 实际入口：仪表盘组件属性配置中的 Aloudata「指标&维度」弹窗。
- 实现截图：`/tmp/aloudata-analysis-config-login.png`（873 × 643 CSS px，deviceScaleFactor 1）。
- 预览地址：`http://127.0.0.1:5181/`；浏览器被路由至 `/login`。

## 状态与阻塞

- 目标状态：Aloudata 指标/维度弹窗内显示分析配置面板，维度和指标均为空，可分别打开选择器。
- 当前可捕获状态：登录页。未将现有 5174 用户会话或认证令牌复制到隔离预览。
- 因实现页面无法进入，未能在同一输入中并排比较来源图和实现页面；完整视图及局部区域视觉核验均阻塞。登录页截图不是目标组件的实现证据，不能据此声称像素级一致。

## 代码与交互检查

- 已实现原型中的 ANALYSIS BUILDER 标题、字段配置编号、维度/指标分区、提示文案、添加按钮、空态框、可删除已选字段块及底部说明。
- 两个添加按钮分别挂接原有实时目录选择器，保留搜索、目录展开、分页、详情悬浮及指标-维度兼容校验；删除字段更新原有选择状态。
- 新增回归测试覆盖空态、分别打开目录、选择维度展示与移除同步。

## Fidelity surfaces

- 字体/排版、间距/布局、颜色/token、图标与内容：代码按来源截图实现；因真实弹窗当前无法在浏览器中打开，尚未做渲染态逐像素比较，均为待视觉核验项。
- P0/P1/P2：当前没有从实际渲染截图可判定的视觉发现；这不代表已通过视觉验收。

## 验证

- `npm test -- --run src/views/insight/components/__tests__/AloudataDialog.spec.ts`：13 项通过。
- `npm run build`：通过（有既有 Rollup 注释与大 chunk 提示）。
- 全量 Vitest：88 个文件中 86 个通过；4 项失败，位于既有 `QueryConfigDialog.spec.ts`、`PythonQueryConfigDialog.spec.ts`，与本次改动路径无关。
- 浏览器交互与视觉对照：BLOCKED，隔离预览被重定向到登录页。

## 后续验收

1. 在隔离预览可登录或提供专用本地测试入口后，于相同 873 × 643 视口打开真实弹窗。
2. 核对完整布局与分析配置面板局部：标题层级、留白、两组区块、空态/已选态、按钮和弹出的实时目录。
3. 更新本报告中的截图、交互记录和最终结果；仅在无 P0/P1/P2 视觉差异后标记 `passed`。
