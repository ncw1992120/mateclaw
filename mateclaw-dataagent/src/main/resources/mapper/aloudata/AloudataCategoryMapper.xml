<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vip.mate.dataagent.repository.AloudataCategoryMapper">

    <insert id="upsertBatch" parameterType="java.util.List">
        INSERT INTO `dataagent_aloudata_category`
        (`id`, `datasource_id`, `category_id`, `category_name`, `category_type`,
         `parent_id`, `sync_version`, `create_time`, `update_time`)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.datasourceId}, #{item.categoryId}, #{item.categoryName}, #{item.categoryType},
             #{item.parentId}, #{item.syncVersion}, #{item.createTime}, #{item.updateTime})
        </foreach>
        ON DUPLICATE KEY UPDATE
            `category_name` = VALUES(`category_name`),
            `category_type` = VALUES(`category_type`),
            `parent_id` = VALUES(`parent_id`),
            `sync_version` = VALUES(`sync_version`),
            `update_time` = VALUES(`update_time`)
    </insert>

</mapper>
